pipeline{
        agent any
        stages{
            stage('Echo ABCD'){
                steps{
                    withcredentials([string (credentialsId: 'ABCD', variable: 'ABCD')]){
                   
                    sh "echo ${ABCD}"
                    }
                }
            }
        }
}
